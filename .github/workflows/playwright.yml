name: Playwright Tests
on:
  workflow_dispatch:
    inputs:
      test_plan_key:
        description: 'Xray Test Plan Key'
        required: false
        default: 'RQT-1'
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      SAUCEDEMO_BASE_URL: ${{ vars.SAUCEDEMO_BASE_URL }}
      SAUCEDEMO_USER_STANDARD: ${{ vars.SAUCEDEMO_USER_STANDARD }}
      SAUCEDEMO_USER_LOCKED_OUT: ${{ vars.SAUCEDEMO_USER_LOCKED_OUT }}
      SAUCEDEMO_USER_PROBLEM: ${{ vars.SAUCEDEMO_USER_PROBLEM }}
      SAUCEDEMO_USER_PERFORMANCE_GLITCH: ${{ vars.SAUCEDEMO_USER_PERFORMANCE_GLITCH }}
      SAUCEDEMO_USER_ERROR: ${{ vars.SAUCEDEMO_USER_ERROR }}
      SAUCEDEMO_USER_VISUAL: ${{ vars.SAUCEDEMO_USER_VISUAL }}
      SAUCEDEMO_PASSWORD: ${{ secrets.SAUCEDEMO_PASSWORD }}
      SAUCEDEMO_PASSWORD_INVALID: ${{ secrets.SAUCEDEMO_PASSWORD_INVALID }}
      SAUCEDEMO_PERF_THRESHOLD_MS: ${{ vars.SAUCEDEMO_PERF_THRESHOLD_MS }}
      XRAY_IMPORT_ENABLED: 'false'
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
        cache: npm
    - name: Install dependencies
      run: npm ci
    - name: Cache Playwright browsers
      uses: actions/cache@v4
      id: playwright-cache
      with:
        path: ~/.cache/ms-playwright
        key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
    - name: Install Playwright system dependencies
      run: npx playwright install-deps
    - name: Install Playwright Browsers
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: |
        MAX_ATTEMPTS=3
        for browser in chromium firefox webkit; do
          echo "Installing $browser..."
          installed=false
          for attempt in $(seq 1 $MAX_ATTEMPTS); do
            if timeout 300 npx playwright install $browser; then
              installed=true
              break
            fi
            if [ $attempt -lt $MAX_ATTEMPTS ]; then
              echo "Attempt $attempt failed for $browser, retrying in 10s..."
              sleep 10
            fi
          done
          if [ "$installed" != "true" ]; then
            echo "::error::Failed to install $browser after $MAX_ATTEMPTS attempts"
            exit 1
          fi
        done
    - name: Run Playwright tests
      run: npx playwright test
      continue-on-error: true
    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 14
    - name: Upload JUnit results artifact
      uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: junit-report
        path: results/junit-report.xml
        retention-days: 14

    # ── Xray Import ──────────────────────────────────────
    - name: Authenticate with Xray Cloud
      if: ${{ !cancelled() && env.XRAY_IMPORT_ENABLED == 'true' }}
      id: xray-auth
      run: |
        RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
          "https://eu.xray.cloud.getxray.app/api/v2/authenticate" \
          -H "Content-Type: application/json" \
          -d "{\"client_id\":\"${{ secrets.XRAY_CLIENT_ID }}\",\"client_secret\":\"${{ secrets.XRAY_CLIENT_SECRET }}\"}")
        
        HTTP_CODE=$(echo "$RESPONSE" | tail -1)
        TOKEN=$(echo "$RESPONSE" | sed '$d' | tr -d '"')
        
        echo "Xray auth HTTP status: $HTTP_CODE"
        
        if [ "$HTTP_CODE" != "200" ] || [ -z "$TOKEN" ]; then
          echo "::error::Xray authentication failed (HTTP $HTTP_CODE)"
          echo "Response: $(echo "$RESPONSE" | sed '$d')"
          exit 1
        fi
        
        echo "::add-mask::$TOKEN"
        echo "token=$TOKEN" >> "$GITHUB_OUTPUT"
        echo "Xray authentication successful"

    - name: Import results to Xray
      if: ${{ !cancelled() && env.XRAY_IMPORT_ENABLED == 'true' && steps.xray-auth.outputs.token != '' }}
      run: |
        TEST_PLAN_KEY="${{ github.event.inputs.test_plan_key || 'RQT-1' }}"
        curl -s -X POST \
          "https://eu.xray.cloud.getxray.app/api/v2/import/execution/junit?projectKey=RQT&testPlanKey=RQT-1" \
          -H "Content-Type: application/xml" \
          -H "Authorization: Bearer ${{ steps.xray-auth.outputs.token }}" \
          -d @results/junit-report.xml

  deploy-report:
    name: Deploy Playwright Report to Pages
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
    needs: test
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
      contents: read
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
    - name: Configure GitHub Pages
      uses: actions/configure-pages@v5
    - name: Download Playwright report artifact
      uses: actions/download-artifact@v4
      with:
        name: playwright-report
        path: playwright-report/
    - name: "Guard: verify Playwright HTML report exists"
      run: |
        if [ ! -f "playwright-report/index.html" ]; then
          echo "Expected playwright-report/index.html but it was not found."
          echo "Contents of playwright-report directory:"
          ls -la playwright-report || true
          exit 1
        fi
    - name: Upload Playwright report for Pages
      uses: actions/upload-pages-artifact@v3
      with:
        path: playwright-report/
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4